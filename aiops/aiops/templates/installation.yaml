apiVersion: orchestrator.aiops.ibm.com/v1alpha1
kind: Installation
metadata:
  namespace: aiops
  name: ibm-cp-watson-aiops
spec:
  imagePullSecret: {{ .Values.imagePullSecret }}
  license:
    accept: true
  pakModules:
    - enabled: {{ .Values.pakModule.aiManager }}
      name: aiManager
    - enabled: {{ .Values.pakModule.aiopsFoundation }}
      name: aiopsFoundation
    - enabled: {{ .Values.pakModule.applicationManager }}
      name: applicationManager
  size: {{ .Values.size }}
  storageClass: {{ .Values.storageClass }}
  storageClassLargeBlock: {{ .Values.storageClassLargeBlock }}
